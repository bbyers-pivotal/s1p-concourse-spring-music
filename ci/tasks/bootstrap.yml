---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: brianbyers/concourse-git

inputs:
  - name: git-repo
  
outputs:
  - name: blue-green-output
run:
  path: git-repo/ci/tasks/bootstrap.sh

params:
  ARTIFACTORY_COLOR_REGEX:
